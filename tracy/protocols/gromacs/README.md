# GROMACS protocol directory

This directory will hold protocol files for the GROMACS MD stage (Milestone 2).

Each protocol file will describe:

- Minimisation, equilibration, and production MDP settings.
- Restraint schedule for membrane equilibration.
- Reference for normal-axis orientation.

Do not add protocol files here until `RunMembraneMDWorkChain` is implemented.
